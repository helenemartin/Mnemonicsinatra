<!DOCTYPE html>
<html>
  <head>
    <title>Mnemonic#9</title>
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
      <div id = "wrapper">
        <header>
          <h1>Mnemonic#9</h1>
        </header>
      <div id = "multiplication">
          <form action="/submit" method="post">
              <textarea name="message" placeholder="Type a number between 0 and 10"><%= if @number>0 then @number end %></textarea>
              <label>X 9</label> 
              <input type="submit" value="Calculate" />
            </p>
          </form>
      </div>

      <div id ="content">
          <% if not @error %>
          <img src= <%= "images/#{@number}.jpg" %>>
          <% end %>
          <% if @error %>
          <img src= <%= "images/animnim.gif?" + Time.now.to_i.to_s %>> 
          <div id="alert">
          <p>Wrong Input, it shouldn't be bigger than 10!</p>
          <% end %>
        </div>
      </div>
          <div class="clear"></div>
    </div>
  </body>
</html>