<!DOCTYPE html>
<html>
  <head>
    <title>Mnemonic#9</title>
    <link rel="stylesheet" href="/css/style.css">
    </head>
  <body>
      <div class="wrapper">
        <header>
          <h1>Mnemonic#9</h1>
        </header>
      
      <div id ="content">
        <div id="multiplication">
          <form action="/submit" method="post">
              <textarea name="message" placeholder="Type a number between 0 and 10"><%= if @number>0 then @number end %></textarea>
              <label>X 9</label> 
              <input type="submit" value="Calculate" />
          </form>
      </div>
        <% if @error %>
        <div id="alert">
          <h6>Wrong Input, it shouldn't be bigger than 10!</h6>
          <img src= <%= "images/animnim.gif?" + Time.now.to_i.to_s %> alt=""> 
        </div>
          <% else %>
          <img src= <%= "images/#{@number}.jpg" %>>
          <% end %>
      </div>
     <!-- <div class="clear"></div> -->
    </div>
  </body>
</html>